# Copy to .env and fill in. .env is gitignored; this template is committed.

# --- Local dev Postgres (matches docker-compose.yml defaults) ---
POSTGRES_USER=audentify
POSTGRES_PASSWORD=change-me
POSTGRES_DB=audentify_dev
DATABASE_URL=postgresql://audentify:change-me@localhost:5432/audentify_dev

# --- Stage 1 external APIs (filled in during Phase 1) ---
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# ACOUSTID_API_KEY=
